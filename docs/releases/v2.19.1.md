# v2.19.1 迭代记录

> 日期：2026-07-13

## 修复与优化

### 1. 桌面端输入框锚定

- 修复终端面板打开时输入框被顶到终端上方的视觉跳跃。
- 输入框随文本增多拉长时，自动保持底部对齐，避免最后一条消息被推出可视区域。

### 2. TUI 测试清零

- 同步近期样式改动后的测试断言：输入框提示符、工具卡片 bullet、compactHints 页脚格式等。
- `src/tui/` 全量测试 1100 pass / 0 fail。

### 3. 更新器健壮性

- CLI 自更新支持 `HTTP_PROXY` / `HTTPS_PROXY` / `NO_PROXY`。
- Windows 全局安装自更新改用绝对 `npm` 路径，避免文件锁释放后找不到 npm。
- 注册表 / GitHub API 调用增加指数退避重试，网络失败时不再写入空缓存。

## 维护说明

CLI、桌面端与 release manifest 同步升至 `v2.19.1`。
