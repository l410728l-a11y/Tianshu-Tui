# main 分支合并前版本快照

> 日期：2026-06-07
> 分支：`backup/v2.9.2-pre-merge`
> Tag：`v2.9.2`

## 定位

这是 `fix/stall-root-causes-abort-exit` 分支上已完成的功能和修复合并回 `main` **之前的** `main` 最终状态。

`fix/stall-root-causes-abort-exit` 分支上累计实现了以下主要功能（见该分支的提交历史）：

- **Server 子系统**：`/prompt` SSE 端点、双层鉴权、H6 断连护栏、go-live gate 全清
- **Intent Retrieval Router**：意图检索路由 + LLM 分类器（默认开启）
- **Review Discipline**：审查纪律内化（ReviewRouter + 重入护栏 + C=C1 默认开启）
- **Async I/O 转换**：18 个工具文件同步 I/O → async
- **死代码清理**：incrementalCommit、unreachable return
- **TUI 渲染修复**：committed-log 引用稳定性、live 区高度约束

## 快照点

```
commit 3d02931
Author: Test <test@test.com>
Date:   Sat Jun 7 03:52:45 2026 +0800
    docs(rules): add semantic disambiguation table for ambiguous Chinese terms
```

## 分支与 Tag

| 名称 | 说明 |
|------|------|
| `backup/v2.9.2-pre-merge` | main 合并前备份分支 |
| `v2.9.2` | 同位置的 tag |

## 恢复方式

```bash
# 查看合并前的 main 状态
git checkout backup/v2.9.2-pre-merge

# 或通过 tag
git checkout v2.9.2
```
