# 天枢现状复盘 — 2026-07-01

> 本会话覆盖: P6 AST 工具全周期 + prompt 减负 + R2 pi 回移植审查 + 子代理评估

## 一、本会话交付（10 笔提交）

| 提交 | 内容 | 领域 |
|------|------|------|
| `45a9bf29` → `9faa7ccb` | P6 核心: ast-grep + ast-edit + 注册 | tools |
| `7db6ad2d` | 共享模块提取: ast-shared + LANG_MAP assert + 原子写 | tools |
| `bc4a7349` | onFileWrite 回调: 证据追踪内部文件写入 | agent+tools |
| `ddca90dc` | hash_edit 加入 pipeline evidence 追踪 | agent |
| `95e1b5a5` | 遗留项文档更新 | docs |
| `2a0842cd` (他会话) | P6 遗留: 多语言 + 重叠去重 + 可配置排除 | tools |
| `3d013535` (他会话) | read-dedup per-session 隔离 | tools |
| `d1443252` (他会话) | includeMeta 输出增强 | tools |

## 二、各子系统状态

### Agent Loop — ✅ 稳定
- 6 阶段工作流 (`①理解→②调研→③拆解→④实施→⑤验证→⑥收尾`) 已落地
- 诊断循环独立保留（与开发循环并存）
- delivery-contract 集中化（规则从 8→6，hard-gates 3→2）
- "不自我设限" 已加入 contract

### Tools — ✅ 本轮大幅增强
- **新增**: ast_grep、ast_edit（含 Python/JSON 动态语言、重叠去重、可配置排除、post-edit 语法门）
- **修复**: hash_edit evidence 追踪、read-dedup per-session、read-ref stats per-session
- **加固**: collectFiles MAX_FILES/MAX_DEPTH 上限、EXCLUDE_DIRS 扩展、meta-var 截断
- 测试: 48 用例全绿

### Prompt — ✅ 减负周期收束
- static.ts: 规则合并 + 6 阶段工作流 + delivery-contract 集中化
- volatile.ts: 英→中本地化
- 不作: 工具策略简化、条件化核心规则
- 子代理 prompt 减负: 已出方案但因 AB 实验数据(有星域人格 100% vs 无 80%)暂缓

### Cache / Compaction — ✅ 本轮增强
- prewarm 死存储复活: 消费侧从零接线, rawContent 全文缓存 + mtime 校验
- 精度天花板: 窗口推导(≥500K→0.5, ≥200K→0.55), 可配置, 不硬编码

### R2 pi 回移植 — ⬜ 剩余项
- ✅ R2-5 prewarm 复活
- ✅ R2-2 精度天花板
- ✅ R2-4 stats per-session
- ✅ R2-1 ephemeral aside (advisoryBus 复用)
- ✅ R2-3 onTurnEnd guard (随 R2-1)
- **全 5 项已落地**

### Sub-Agent / Team — ✅ 可用, 未深度验证
- team_orchestrate 已接线
- worker prompt 完整继承主 prompt（~250 行）
- 子代理质量: AB 实验已验证星域人格对 Flash 模型 80%→100% 提升
- **未做**: real-task team pipeline 端到端测试

### TUI / Desktop — ⬜ 活跃开发中
- 多个 desktop 文件在 modified 状态（App.tsx, Composer, Rail, ReviewPanel 等）

## 三、优先级排序的 Next Actions

### 可立即做（无需计划，改动小）

1. **P6 includeMeta 行数统计**（P2-5 剩余）
   - `$$$BODY` 值后追加 `(N lines)`, ~5 行代码
   - 风险低, 直接改

2. **`collectFiles` 排除列表文档化**
   - 当前 `resolveExcludeDirs()` 已支持 `RIVET_AST_EXCLUDE` 环境变量, 但没在任何文档中说明
   - 补一行 README 或 tool description

### 需计划（中等复杂度）

3. **子代理 prompt 差异化精简**（已有方案, 待 AB 验证后启动）
   - 分 profile: 审查类保留星域人格, 执行类精简
   - 风险: 需先用真实 team 任务 A/B 验证 Flash 子代理有/无星域人格的差异

4. **P6 端到端集成测试**（P3-8 剩余）
   - 模型 → tool call → execute → 验证 完整链路
   - 可先做一个最小版本（ast_grep 读文件→匹配→断言）

### 可搁置（大变更，非紧急）

5. **完整 onToolComplete 回调**
   - 当前 onFileWrite 已覆盖最急缺项
   - 完整版需框架层变更, ROI 边际

6. **`collectFiles` 排除列表可配置 UI**
   - 已有 env var 方案, UI 配置属锦上添花

## 四、无 immediate concern 的区域

- **security**: 破坏性命令闸门、敏感文件保护、系统消息信任边界 — 稳定
- **git/worktree**: 多会话共享工作区规则、deliver_task 门禁 — 稳定
- **delegation**: coordinator/worker-session/work-order — 稳定
- **verification**: 三级验证 + deliver_task 自动接线审查 — 稳定
